import gsap from "gsap";

gsap.to(".button-90", {
    opacity: 1, 
    duration: 3,
    ease: 'power4.out',
})

gsap.to(".product-card", {
    opacity: 1,
    duration: 3,
    ease: 'power2.inOut',
})